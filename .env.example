# Pick one backend; the agent reads whichever keys it needs.
DECISIONCIRCUITS_API_KEY=    # --backend circuits   the default; optional: a free key is issued if empty
TYPESAFE_API_KEY=            # --backend jev        https://typesafe.ai
S1_URL=http://localhost:8901/v1/systemone   # --backend local
S1_API_KEY=local
LANGSMITH_API_KEY=           # --backend semif
OPENAI_API_KEY=              # --backend openai
ANTHROPIC_API_KEY=           # --backend anthropic
